<div class="container">
  <div class="row py-4 pr-3">
    <div class="col-md-12">
      <h2 class="text-center mb-4 mt-3">ユーザー一覧</h2>
    </div>
  </div>
  <div class="row">
    <div class="col-7 mx-auto">
      <table class='table table-hover'>
        <thead>
          <th>ユーザー名</th>
          <th>投稿数</th>
        </thead>
        <% @users.each do |user| %>
        <tbody>
          <tr>
            <td><%= link_to "#{user.last_name + user.first_name}", user_path(user.id) %></td>
            <td><%= user.posts.count %></td>
          </tr>
        </tbody>
        <% end %>
      </table>
    </div>
  </div>
</div>