<h1>
  投稿詳細
</h1>
<div>
  <%= image_tag @post.get_image(500, 300) %>
  <p>商品名：<%= @post.name %>　</p>
  <p>一言：<%= @post.title %></p>
  <p>商品説明：<%= @post.body %></p>
  <p>♥：1　コメント：3</p>
  <p>投稿日：<%= @post.created_at.strftime('%Y/%m/%d') %></p>
  <% if @post.user = current_user %>
    <%= link_to "編集する", edit_post_path %>
  <% end %>
  <%= link_to "一覧へ戻る", posts_path %>
  <% if @post.user = current_user %>
    <%= link_to "削除する", post_path(@post.id), method: :delete %>
  <% end %>
</div>